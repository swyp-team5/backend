spring:
  datasource:
    url: jdbc:tc:mysql:8.4.0:///autoschedule_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: test
    password: test

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

  sql:
    init:
      mode: never

logging:
  level:
    org.springframework.security: warn
    org.hibernate.SQL: warn
